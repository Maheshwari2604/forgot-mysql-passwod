
1 - For CentOS and Fedora, type:

    	service mysqld stop

1 - For Debian and Ubuntu, type:

    	service mysql stop

2 - mysqld_safe --skip-grant-tables &

3 - mysql.user SET Password=PASSWORD('maheshwari') WHERE User='root';

4 - FLUSH PRIVILEGES;

5 - exit;

6 - enter = mysql -u root -p
# it will ask for password
password: maheshwari

Successfully logged in
